# Docs

Drop a demo recording at `docs/demo.gif` (or `.mp4`/`.webm`) and the link in
the root `README.md` will resolve automatically. Keep this lightweight — a
10–15s GIF showing one debate round is plenty.
